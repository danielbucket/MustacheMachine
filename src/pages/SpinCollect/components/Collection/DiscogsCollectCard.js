export default class DiscogsCollectCard {
	constructor({ id, master_id, master_url, resource_url, thumb}) {
		this.id = id
		this.master_id = master_id
		this.master_url = master_url
		this.resource_url = resource_url
		this.thumb = thumb
	}
}